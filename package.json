{
  "name": "nested-json",
  "version": "0.1.0",
  "description": "process nested json",
  "main": "",
  "scripts": {
    "compile": "npm run clean && npm run lint; rm -rf dist/; tsc -p .",
    "clean": "rm -rf dist/",
    "watch": "tsc -w -p .",
    "test": "jest",
    "lint": "tslint --project tsconfig.json",
    "lint-fix": "tslint --fix --project tsconfig.json",
    "format": "prettier --write src/**/*.ts"
  },
  "keywords": [],
  "author": "gerlacdt",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^23.0.0",
    "@types/node": "^10.1.4",
    "jest": "^23.1.0",
    "prettier": "^1.13.4",
    "tslint": "^5.9.1",
    "typescript": "^2.9.1"
  },
  "dependencies": {}
}
